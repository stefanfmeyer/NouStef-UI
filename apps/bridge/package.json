{
  "name": "@hermes-recipes/bridge",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_OPTIONS=--experimental-sqlite tsx src/server.ts --port 8787",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "start": "NODE_OPTIONS=--experimental-sqlite tsx src/server.ts",
    "start:fixture": "NODE_OPTIONS=--experimental-sqlite tsx src/server.ts --fixture",
    "test:live:home": "tsx scripts/live-home-workspace-validation.ts"
  },
  "dependencies": {
    "@hermes-recipes/protocol": "workspace:*",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "tsx": "^4.19.3"
  }
}
